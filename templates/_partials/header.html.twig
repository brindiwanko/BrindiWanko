<nav class="navbar navbar-expand-md navbar-dark fixed-top bg-dark">
    <div class="container-fluid">
        <a class="navbar-brand" href="{{ path('app_home') }}">{{ app_parameter('app.name') }}</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarsExampleDefault" aria-controls="navbarsExampleDefault" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarsExampleDefault">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="dropdown08" data-bs-toggle="dropdown" aria-expanded="false">Accueil</a>
                    <ul class="dropdown-menu" aria-labelledby="dropdown08">
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Actualité</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Présentation</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Les classes</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Contact</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">A propos</a></li>
                    </ul>
                </li>
            </ul>
            <ul class="navbar-nav pull-right">
                <li class="nav-item dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="dropdown08" data-bs-toggle="dropdown" aria-expanded="false">Mon compte</a>
                    <ul class="dropdown-menu" aria-labelledby="dropdown08">
                        {% if is_granted('ROLE_MODERATOR') %}
                            <li><a class="dropdown-item" href="{{ path('app_home') }}">Modération</a></li>
                        {% endif %}
                        {% if is_granted('ROLE_ADMIN') %}
                            <li><a class="dropdown-item" href="{{ path('app_home') }}">Administration</a></li>
                        {% endif %}
                        <li><a class="dropdown-item" href="{{ path('login') }}">Connexion</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Inscription</a></li>
                        <li><a class="dropdown-item" href="{{ path('app_home') }}">Code</a></li>
                        {% if is_granted('IS_AUTHENTICATED') %}
                            <li><a class="dropdown-item" href="{{ path('logout') }}">Déconnexion</a></li>
                        {% endif %}
                    </ul>
                </li>
            </ul>
        </div>
    </div>
</nav>
