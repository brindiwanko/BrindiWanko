<form method="post" action="{{ path('app_town_delete', {'id': town.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ town.id) }}">
    <button class="btn">Delete</button>
</form>
