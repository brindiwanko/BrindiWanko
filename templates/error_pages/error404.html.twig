{# templates/bundles/TwigBundle/Exception/error4xx.html.twig #}
{% extends 'error_pages/base.html.twig' %}

{% block title %}Oops! Something went wrong{% endblock %}

{% block body %}
    <div class="error-page">
        <h1>🤔 {{ status_code }} - Client Error</h1>
        <p>{{ status_text|default('Something went wrong with your request.') }}</p>

        {% if status_code == 403 %}
            <p>You don’t have permission to access this resource.</p>
        {% elseif status_code == 404 %}
            <p>The page you are looking for could not be found.</p>
        {% endif %}

        <a href="{{ path('app_home') }}">← Back to homepage</a>
    </div>
{% endblock %}